import 'package:flutter/material.dart';

class AppColors {
  // Premium fitness palette
  static const Color primary = Color(0xFF2B2DFF); // Deep Indigo
  static const Color accent = Color(0xFF00D4FF);  // Electric Cyan

  // Light surfaces
  static const Color lightBg = Color(0xFFF6F7FB);
  static const Color lightSurface = Color(0xFFFFFFFF);

  // Dark surfaces (graphite)
  static const Color darkBg = Color(0xFF0B0F14);
  static const Color darkSurface = Color(0xFF101826);
  static const Color darkSurface2 = Color(0xFF131F31);

  // Semantic
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color danger = Color(0xFFEF4444);

  // Text helpers
  static const Color onDark = Color(0xFFEAF0FF);
  static const Color onLight = Color(0xFF0B1220);
}